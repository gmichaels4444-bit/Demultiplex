420250721
Part 1:
Headed each file
R1 and R4 are sequences, R2 and R3 are barcodes that are mostly reverse complemnented

1i. 
| File | Label|
| --- | --- |
| 1294_S1_L008_R1_001.fastq.gz | read1 | 
| 1294_S1_L008_R2_001.fastq.gz | index1 |
| 1294_S1_L008_R3_001.fastq.gz | index2 |
| 1294_S1_L008_R4_001.fastq.gz | read2 |


wc -l: 1452986940; divide by 4 to get 363246735 individual reads
  All files are the same length
 1ii. 
 zcat 1294_S1_L008_R1_001.fastq.gz | head -2 | tail -1 | wc
  101 (102 counting newline)
 zcat 1294_S1_L008_R2_001.fastq.gz | head -2 | tail -1 | wc
  8 (9 counting newlines)
  phred +33 


Demultiplexing:
Each header from each file matches, but indices are not found in the full length reads.
  Indices with the same header in R2 and R3 are generally reverse compliments
  All or almost all indices have an "N" at the first position
  Will need to reverse match internal 6 positions of each index to determine index matchiong
    Write a reverse complement function for this
Will need to 




Created test fastq files from each fastq file using  ```head -20 <file>```
Edited index files and qscores to have entries with matching barcodes, hopped barcodes, Ns, and barcodes that were not present in the indices

**** finish this later***


Assignment the first part 1:
Perform some initial data exploration! Record any bash commands you used inside a lab notebook (submit to this repo!).

Indices are in R2 and R3, with R3 mostly appearing to be 
Determine which files contain the indexes, and which contain the paired end reads containing the biological data of interest. Create a table and label each file with either read1, read2, index1, or index2.
Determine the length of the reads in each file.
Determine the phred encoding for these data.
Generate a per base distribution of quality scores for read1, read2, index1, and index2. Average the quality scores at each position for all reads and generate a per nucleotide mean distribution as you did in part 1 of PS4 in Bi621. (NOTE! Do NOT use the 2D array strategy from PS9 - you WILL run out of memory!)
Turn in the 4 histograms.
What is a good quality score cutoff for index reads and biological read pairs to utilize for sample identification and downstream analysis, respectively? Justify your answer.
How many indexes have undetermined (N) base calls? (Utilize your command line tool knowledge. Submit the command(s) you used. CHALLENGE: use a one-line command)

